import backtrader as bt
import datetime

class MADIFF(bt.Indicator):

    lines = ('diff',)
    params = (
        ('period', 20),
    )
    def __init__(self):
        self.
        self.lines.diff =
